def linear_search(list, target):
        # return index position of target if found, else returns None
        
        for i in range(0, len(list)):
            if list[i] == target:
                return print(i)
        return None
    

linear_search([1,2,3,4,5,6,7,8], 6)